#include <stdio.h>
int main()
{
// A SIMPLE PROGRAM TO ADD TWO NUMBERS
int fnum = 10;
int lnum = 13;
int x = fnum + lnum;

//x = fnum + lnum;

printf("the sum of fnum + lnum is %d\n",x);

return 0;

}
